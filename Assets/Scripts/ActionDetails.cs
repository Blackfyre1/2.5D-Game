using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class ActionDetails : PieceDetails
{
    public bool Projectile;
    public bool Attach;
    public bool MoveTo;
    public bool Trap;
    public bool
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
